<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorWhite"
    tools:context=".ui.search.complete.SearchCompleteFragment">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycleSearchContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

    <TextView
        android:id="@+id/tvResultEmptyFind"
        android:text="@string/find_0"
        style="@style/TextEmpty"
        android:visibility="gone"/>

    <ProgressBar
        android:id="@+id/progressLoadmore"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:indeterminateTint="@color/colorMain"
        android:layout_gravity="center"/>

</FrameLayout>